/**
 * 功能：工程内配置
 * 作者：安超
 * 日期：2017/6/29
 */

import errors from './errors';
import url from './url';
import * as constant from './constant';

const config = {
    errors,
    constant,
    url
};

export default config;
